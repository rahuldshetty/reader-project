name: Deploy GitHub Pages on Release

on:
  release:
    types: [published] # after the release exists, so releases/latest/download/* resolves
  workflow_dispatch: # deploy docs edits without cutting a release

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs
          # v4+ excludes dotfiles by default; keep docs/.nojekyll (still served today)
          include-hidden-files: true

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
